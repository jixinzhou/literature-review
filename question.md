1、openalex检索的都是英文文献，但是对于中国的学生来说也需要部分的国内期刊的文献；你可以拿中英文检索词分别检索，但是中文文献也需要出现在里面；这个怎么去权衡工作流？
2、参考文献的格式需要遵循GB/T 7714-2015；
这两个问题切中了中国学术环境的真实需求。要让这个 MVP 从“能用”变成“好用”，我们需要在工作流中加入**“语言分流”**逻辑和**“格式标准化”**模块。

以下是针对这两个问题的深度解决方案：

---

## 1. 中英文文献权衡工作流 (Bilingual Balance)

虽然 OpenAlex 涵盖了大量带有 DOI 的中文核心期刊（如《计算机学报》、《机械工程学报》等），但其总量确实少于英文。

### 🚀 改进的检索策略：并行双轨制
不要合并成一个检索词，而是**分两次独立请求**，最后在内存中合并。

* **步骤 2.1（英文检索）**：调用 `search_queries.history_background.en`，设置 `filter=language:en`（收窄为英文文献）。
* **步骤 2.2（中文检索）**：调用 `search_queries.history_background.cn`，**仅用中文检索词全文检索**，不设 `language:zh`（OpenAlex 上该过滤常无效）；本地对结果做中文优先排序。
* **权衡比例建议**：
    * **默认权重**：推荐 **3:7 (中:英)**。因为前沿技术（如 GNN）的源头论文多为英文，而“国内研究现状”部分必须有中文文献支撑。
    * **动态配额**：在代码逻辑中设置：如果中文文献不足 5 篇，自动调取“历史背景组”的中文检索式进行补充。

### 🛠️ 给开发者的逻辑更新：
> "在执行第二步时，后端需要发起两次并发请求。一次针对英文关键词，一次针对中文关键词。在第三步打分前，确保结果集中至少包含 30% 的语言标识为 'zh' 的文献。如果 OpenAlex 返回的中文文献质量（被引量）较低，适当降低中文文献的 '权威性 (Sauth)' 权重，以保证它们能排进前 15 名。"

---

## 2. 遵循 GB/T 7714-2015 格式

这是中国学术论文的硬性门槛。OpenAlex 返回的是碎片化数据（作者、标题、刊名、卷号等），直接让 AI 生成格式容易出错。

### 🛠️ 解决方案：AI 辅助格式化
在第四步发给 Qwen 的提示词中，增加一个专门的 **“格式化协议”**。

### 修改后的第四步 AI 提示词（局部强化）：
> **# 参考文献规范 (Strict Formatting)**
> 请务必将文末的参考文献按照 **GB/T 7714-2015** 标准格式排列。格式模板如下：
> - **期刊 [J]**: [序号] 主要责任者. 题名: 其他题名信息 [J]. 刊名, 出版年, 卷(期): 页码. DOI.
> - **学位论文 [D]**: [序号] 主要责任者. 题名 [D]. 保存地: 保存单位, 年份.
> - **会议论文 [C]**: [序号] 主要责任者. 题名 [C] // 会议名称. 出版地: 出版者, 出版年: 页码.
> 
> **注意**：如果元数据缺失（如缺失页码），请在保证格式完整的前提下略过，但作者、题名、年份必须准确。

---

## 📈 最终版产品架构评价（针对程序员）

如果你现在把这份完整的文档（含这两项更新）交给 AI 程序员，**实现难度依然处于“中等”**。

### 程序员需要处理的难点清单：
1.  **OpenAlex 的语言过滤**：需要在 URL 参数中加入 `filter=languages:zh`。
2.  **多作者处理**：GB/T 7714 要求前三位作者姓名，超过三个需加“, 等”或“, et al.”。这需要后端在发给 Qwen 之前，先用一段 Python 代码对 `authorships` 字段做预处理。
3.  **文献类型标识**：程序需要判断文献是 `article` (对应 [J]) 还是 `dissertation` (对应 [D])。

### 💡 核心代码逻辑补丁（建议加入文档）：
```python
# 给程序员的逻辑参考：GB/T 7714 作者处理
def format_gb7714_authors(authors_list):
    names = [a['author']['display_name'] for a in authors_list]
    if len(names) <= 3:
        return ", ".join(names)
    else:
        return f"{names[0]}, {names[1]}, {names[2]}, 等"
```

---
