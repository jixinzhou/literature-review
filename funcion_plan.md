#总目标
我需要做一个自动生成国内外研究现状的网页功能
#任务拆解：
第一步：根据用户的输入调用大模型实现意图理解生成检索词
第二步：根据检索词条用openalex的API去爬取标题、摘要、引用、发表年限、引用格式；
第三步：对找寻的文献进行筛选：按比例打分，后台需要做筛选，近五年的文献要占50%，引用量，这里需要用户确认才能进入下一步；确认标题、摘要。
第四步：将提取出的10-15篇文献写一个国内外研究现状；
#各步骤的具体细节
##第一步：采用问题+场景+方法的模式理解意图生成检索词
1、意图理解生成检索词的AI提示词如下：
<# Role
你是一个高精度的学术意图解析引擎。你的任务是将用户模糊的研究描述拆解为结构化的研究要素，并为学术 API（如 OpenAlex, Semantic Scholar）生成专业的布尔检索式。

# Task
1. **语义拆解**：从输入中识别出研究问题 (Problem)、应用场景 (Scene)、以及所用的新方法 (Method)。
2. **检索式构造**：生成**一路**检索策略——仅聚焦于**研究问题 + 应用场景**（与“创新趋势/新方法”无关），用于检索该问题在相关场景下的既有研究，避免文献主题发散、混杂。

# Rules
- 必须同时生成中文和英文关键词（学术检索以英文为主）。
- 使用标准的布尔逻辑：AND, OR, 以及引号 "" 用于精确匹配。
- 检索式应尽量涵盖同义词以提高查全率。
- **输出格式必须为纯 JSON，严禁包含任何解释性文字。**

# JSON Schema
{
  "research_topic": "一句话研究主题（综述占位）",
  "analysis": {
    "problem": "核心研究问题",
    "scene": "应用场景/行业",
    "method": "用户提出的新技术/方法（仅理解语境，不单独用于检索）"
  },
  "search_queries": {
    "history_background": {
      "en": "English Boolean Query for Problem + Scene ONLY",
      "cn": "中文布尔检索式：仅 问题 + 场景"
    }
  }
}>
2、AI意图理解模型采用，先采用qwen最新模型，我到时候提供API给你
##第二步：openalex的调用
1、这里给我留个填写API的地方
2、这一步的准确性在于阅读技术网址：https://developers.openalex.org/api-reference/introduction
##第三步：文献筛选算法
你需要为我写一个筛选算法，这个算法我觉得先做一个最小MVP:
1、采用打分机制，近五年的文献占权重
时间权重 (Srec​),50%,"Srec​=max(0,1−10Age−5)",Age≤5 年为满分；超过后随年限线性衰减。
权威性 (Sauth​),50%,"Sauth​=ln(1+Cmax,year​)ln(1+Ci​)​",对被引量 C 取对数归一化，消除“马太效应”导致的极端值。
2、流程：抓取（比如 50 篇） -> 计算得分 -> 排序 -> 取前 15 篇 -> 展示给用户确认；
##第四步：生成国内外研究现状
1、将筛选好的文献的摘要、标题、引用格式一起发给另一个qwenAI大模型，用于生成带参考文献和引用的国内外研究现状；
2、这个AI的提示词如下所示：
<# Role
你是一位资深的学术论文审稿人和撰稿专家，擅长撰写逻辑严密的“国内外研究现状”。

# Task
请基于我提供的【文献列表】（包含标题、摘要和引用格式），为我的研究题目《{{研究题目}}》撰写一段“国内外研究现状”。

# Writing Requirements
1. **结构逻辑**：请不要按篇目罗列，而是通过“研究问题”和“技术演进”将文献串联起来。
2. **三步走叙述**：
   - 首先，简述该领域早期或基础的研究现状。
   - 其次，重点描述近年来学者们采用的主要方法及其实践效果。
   - 最后，指出当前研究的不足或趋势，从而引出本研究的必要性。
3. **严格引用**：每一处观点陈述后必须紧跟对应的角标，如 [1]、[2,3]。角标必须与文末的【参考文献】序号一一对应。
4. **语言风格**：学术规范、客观、严谨。使用如“XX 等人提出xxx方法，解决xx问题”、“针对...问题，XX 研究了...”等标准句式。采用某人用某方法解决了某问题的方式；

# Input Data
{{此处粘贴你的文献列表：标题 + 摘要 + 引用格式}}

# Output Format
1. 【国内外研究现状正文】
2. 【参考文献列表】（按正文出现顺序排列）>

#注意点
1.OpenAlex 的“摘要恢复”问题（最关键的技术细节）OpenAlex 为了版权保护，返回的摘要不是纯文本，而是 abstract_inverted_index（倒排索引）。
2. 筛选算法的参数量化你：当前抓取到的这批样本中的最大引用值，这样打分才具有相对意义。算法权重融合：最终总分 $Score = (S_{rec} \times 0.5) + (S_{auth} \times 0.5)$。
